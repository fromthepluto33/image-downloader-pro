# image-downloader-pro
Chrome extension that finds and downloads all images from a page, including hidden ones in backgrounds, SVG and Shadow DOM. Filter by type and size, area selection, ZIP or single download, format conversion. Works entirely locally, no data collection.
